# UMF 超声波流量计 Modbus RTU 通信协议文档

**版本**: v2.3.0
**日期**: 2026-09-11
**适用固件**: UMF_SOFTWARE (STM32F103C8T6)

---

## 1. 通信参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 物理层 | RS-485 | 半双工，PA1 为方向控制引脚 |
| 模式 | Modbus RTU | 二进制传输，非 ASCII |
| 从站地址 | **2** (范围 1~247) | 可通过 FC06/FC10 修改并持久化 |
| 波特率 | **115200** | 支持 4800/9600/19200/38400/115200 |
| 数据位 | 8 | |
| 校验位 | 无 (None) | |
| 停止位 | 1 | |
| 最小帧间隔 | ≥ 3.5 字符时间 | 波特率 115200 时约 0.3ms |

**默认出厂通信参数**: 地址=2, 波特率=115200, 8N1

---

## 2. 支持的功能码

| 功能码 (HEX) | 名称 | 说明 |
|:---:|------|------|
| 0x01 | 读线圈 (Read Coils) | 读取位控制状态 (DAC使能/清零/复位等) |
| 0x03 | 读保持寄存器 (Read Holding Registers) | 读取所有参数和运行数据 |
| 0x04 | 读输入寄存器 (Read Input Registers) | 预留，当前返回空数据 |
| 0x05 | 写单个线圈 (Write Single Coil) | 控制线圈动作 (DAC校准/清零/复位) |
| 0x06 | 写单个寄存器 (Write Single Register) | 写入 uint16 参数或 float 的一个字 |
| 0x10 | 写多个寄存器 (Write Multiple Registers) | 批量写入参数（推荐用于 float） |

---

## 3. 寄存器地址映射表

> **地址约定**: 本文档使用 **Modbus 4X 寻址**（PLC 地址 = 寄存器地址 + 40001），例如地址 0 = 40001，地址 60 = 40061。
> **CRC 校验**: 所有帧末尾 2 字节为 CRC16-Modbus (低字节在前，高字节在后)。

### 3.1 传感器原始数据区 (地址 0~18，只读)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 说明 |
|:---:|:---:|--------|:---:|:---:|------|
| 0~1 | 40001~40002 | 瞬时流量 FlowRateValue | float (2 regs) | R | 固定 L/h，IEEE 754 单精度浮点 |
| 2~3 | 40003~40004 | 温度 FlowTemperature | float (2 regs) | R | 单位 ℃ |
| 4~5 | 40005~40006 | 压力 FlowPressure | float (2 regs) | R | 单位 KPa |
| 6~17 | 40007~40018 | 保留 | — | R | InputBuffer[3~9]，未使用 |
| 18 | 40019 | 保留 | — | R | InputBuffer 区域末尾 |

> **注意**: 地址 0~18 是 InputBuffer 连续区域，FC03 会直接从数组读取。不支持部分地址读取（如只读地址 1），需从偶数地址开始读取完整 float。

### 3.2 累积流量 (地址 40，只读，特殊格式)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 说明 |
|:---:|:---:|--------|:---:|:---:|------|
| 40~43 | 40041~40044 | 累积流量 Cumulativeflow | uint64 (4 regs) | R | BCD 原始计数，LSB=0.001 L/m³；仅在 startaddress=40 时响应 |

> **特殊**: 累积流量使用 uint64 格式（8 字节），仅当 FC03 请求的起始地址恰好为 40 时才返回此数据。该地址与地址 2~3 (温度) 在 InputBuffer 区域无关联，是独立的特殊路径。
>
> **单位标志**: UFL-1A 帧 byte[8] 为 `0x0a` 时，原始计数 LSB=0.001 L；为 `0x1a` 时，原始计数 LSB=0.001 m³。Modbus 路径直传该计数，不按 40024 换算。上位机应根据传感器单位标志解释数值并自行除以 1000。

### 3.3 DAC 校准区 (地址 20~21)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 说明 |
|:---:|:---:|--------|:---:|:---:|------|
| 20 | 40021 | DAC 零点值 DacZeroValue | uint16 | R/W | 4mA 对应的 PWM 原始值 |
| 21 | 40022 | DAC 满度值 DacFullValue | uint16 | R/W | 20mA 对应的 PWM 原始值 |

> **注意**: 修改 DAC 值需要先通过 FC05 (线圈 0) 使能 DAC 校准模式 (CalEnabledFlag=1)。校准模式下写入地址 20 会将 DacValue 设为零点值，写入地址 21 会设为满度值。写入后自动持久化到 Flash。

### 3.4 运行参数区 (地址 22~29)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 默认值 | 有效范围 |
|:---:|:---:|--------|:---:|:---:|:---:|------|
| 22 | 40023 | 流量单位 flow_unit | uint16 (enum) | R/W | 0 (m³/h) | 0~3；控制 OLED 显示换算，不改变 40001 |
| 23 | 40024 | 累积单位 total_unit | uint16 (enum) | R/W | 0 (m³) | 0~3；控制 OLED 显示换算，不改变 40041 |
| 24~25 | 40025~40026 | 仪表系数 meter_coeff | float (2 regs) | R/W | 1.000 | 0.001~99.999 |
| 26~27 | 40027~40028 | 介质系数 medium_coeff | float (2 regs) | R/W | 1.000 | 0.100~10.000 |
| 28~29 | 40029~40030 | 小信号切除 small_signal | float (2 regs) | R/W | 2.0 (%) | 0.0~10.0 |

### 3.5 量程映射区 (地址 30~33)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 默认值 | 有效范围 |
|:---:|:---:|--------|:---:|:---:|:---:|------|
| 30~31 | 40031~40032 | 4mA 量程值 SpanLoValue | float (2 regs) | R/W | 0.0 | -9999.0~99999.0 |
| 32~33 | 40033~40034 | 20mA 量程值 SpanHiValue | float (2 regs) | R/W | 100.0 | 0.1~99999.0 |

> **说明**: 4mA 量程值对应 DAC 零点输出时的流量值，20mA 量程值对应 DAC 满度输出时的流量值。修改后自动持久化到 Flash Page 63。

### 3.6 模拟参数区 (地址 48~55)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 说明 |
|:---:|:---:|--------|:---:|:---:|------|
| 48 | 40049 | 模拟总开关 sim_switch | uint16 | R/W | 0=OFF (使用真实数据), 1=ON (使用模拟数据) |
| 49 | 40050 | — | — | — | 保留间隔 |
| 50~51 | 40051~40052 | 模拟瞬时流量 | float (2 regs) | R/W | 仅在总开关 ON 时有效 |
| 52~53 | 40053~40054 | 模拟温度 | float (2 regs) | R/W | 仅在总开关 ON 时有效 |
| 54~55 | 40055~40056 | 模拟累积流量 | float (2 regs) | R/W | 仅在总开关 ON 时有效 |

> **模拟功能**: 开启总开关 (40049=1) 后，OLED 显示和 DAC 输出将跟随模拟值，用于调试和演示。关闭总开关 (40049=0) 恢复真实传感器数据。模拟参数仅存于 RAM，掉电自动重置为关闭状态。

### 3.7 扩展参数区 — 第一批：运行数据 (地址 60~68，只读)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 说明 |
|:---:|:---:|--------|:---:|:---:|------|
| 60 | 40061 | 通信状态 ModuleState | uint16 | R | 0=通信正常, 非0=模组通信异常 |
| 61~62 | 40062~40063 | 正向累积 forward_total | float (2 regs) | R | 累计正流量 |
| 63~64 | 40064~40065 | 反向累积 reverse_total | float (2 regs) | R | 累计反流量 |
| 65~66 | 40066~40067 | 净累积 (正-反) | float (2 regs) | R | 计算值: forward - reverse |
| 67~68 | 40068~40069 | 实时 4-20mA 电流 | float (2 regs) | R | 当前 DAC 输出电流 (4.0~20.0 mA) |

> **通信状态判断**: ModuleState 由 TIM3 定时检查，超过 500ms 未收到模组回复则置 1，正常接收后清 0。

### 3.8 扩展参数区 — 第二批：配置参数 (地址 69~86)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 默认值 | 有效范围 |
|:---:|:---:|--------|:---:|:---:|:---:|------|
| 69 | 40070 | 标准工况 std_cond | uint16 (enum) | R/W | 0 | 0~2，见枚举表 |
| 70~71 | 40071~40072 | 滤波参数 filter_time | float (2 regs) | R/W | 1.0 | 0.1~100.0 秒 |
| 72~73 | 40073~40074 | 阻尼时间 damping_time | float (2 regs) | R/W | 1.0 | 0.1~100.0 秒 |
| 74~75 | 40075~40076 | 频率输出 freq_output | float (2 regs) | R/W | 1000.0 | 0~10000 Hz |
| 76 | 40077 | 脉冲当量 pulse_equiv | uint16 (enum) | R/W | 0 | 0~5，见枚举表 |
| 77~78 | 40078~40079 | 介质密度 medium_density | float (2 regs) | R/W | 1000.0 | 0.001~99999.0 kg/m³ |
| 79~80 | 40080~40081 | 管径 pipe_diameter | float (2 regs) | R/W | 25.0 | 0.1~99999.0 mm |
| 81~82 | 40082~40083 | 气参压力 gas_ref_press | float (2 regs) | R/W | 101.3 | 0.0~99999.0 KPa |
| 83~84 | 40084~40085 | 气参温度 gas_ref_temp | float (2 regs) | R/W | 20.0 | -40.0~200.0 ℃ |
| 85~86 | 40086~40087 | 雷诺系数 reynolds_k | float (2 regs) | R/W | 1.000 | 0.001~10.000 |

> **持久化**: 所有配置参数写入后自动通过 `param_storage` 模块持久化到 Flash，掉电不丢失。

### 3.9 扩展参数区 — 第三批：系统参数 (地址 87~93)

| 寄存器地址 | Modbus 4X | 参数名 | 数据类型 | 读写 | 默认值 | 有效范围 |
|:---:|:---:|--------|:---:|:---:|:---:|------|
| 87~88 | 40088~40089 | 累积系数 total_factor | float (2 regs) | R/W | 1.000 | 0.001~99.999 |
| 89~90 | 40090~40091 | 预设总量 preset_total | float (2 regs) | R/W | 0.0 | 0.0~9999999.0 |
| 91 | 40092 | 通信地址 modbus_addr | uint16 | R/W | 2 | 1~247 |
| 92 | 40093 | 波特率 baud_rate | uint16 (enum) | R/W | 4 | 0~4，见枚举表 |
| 93 | 40094 | 语言 language | uint16 (enum) | R/W | 0 | 0~1，见枚举表 |

> **通信地址修改**: 写入新地址后立即生效，当前 Modbus 连接将中断。上位机需自行切换到新地址才能继续通信。新地址自动持久化到 Flash。

> **波特率修改**: 写入后采用延迟应用机制 — 先在当前波特率下完成 Modbus 响应帧发送，再切换到新波特率。上位机需在收到响应后切换自身波特率。新波特率自动持久化到 Flash。

---

## 4. 线圈地址映射表 (FC01 / FC05)

线圈通过 FC01 读取状态，通过 FC05 写入控制。FC05 线圈值为 `0xFF00` (ON) 或 `0x0000` (OFF)。

| 线圈地址 | 功能 | ON (0xFF00) | OFF (0x0000) | 说明 |
|:---:|------|-------------|--------------|------|
| 0 | DAC 输出使能 | 使能 DAC 校准模式 (CalEnabledFlag=1) | 禁用 DAC 校准模式 | 使能后方可修改 DAC 零点/满度 |
| 1 | 强制 DAC 零点输出 | DacValue=DacZeroValue, ForceDacOutFlag=1 | 取消强制，恢复正常输出 | 校准用: 强制输出 4mA |
| 2 | 强制 DAC 满度输出 | DacValue=DacFullValue, ForceDacOutFlag=1 | 取消强制，恢复正常输出 | 校准用: 强制输出 20mA |
| 3 | 累积流量清零 | 触发 FlowClearCmdFlag=1 | 清除标志 | 向 UFL-1A 模组发送清零命令 |
| 4 | 流量模组复位 | 触发 FlowRstCmdFlag=1 | 清除标志 | 向 UFL-1A 模组发送复位命令 |
| 5 | 被动读取模式 | 切换为被动读取 (模组不主动发送) | 切换为主动读取 (模组主动发送) | 影响 USART1 与模组的通信方式 |

---

## 5. 枚举值定义

### 5.1 标准工况 (std_cond, 地址 69)

| 值 | 含义 |
|:---:|------|
| 0 | 101.3 KPa / 0℃ |
| 1 | 101.3 KPa / 20℃ |
| 2 | 101.3 KPa / 25℃ |

### 5.2 流量单位 (flow_unit, 地址 22)

| 值 | 含义 | 单位 |
|:---:|------|------|
| 0 | 立方米每小时 | m³/h |
| 1 | 升每小时 | L/h |
| 2 | 升每分钟 | L/min |
| 3 | 千克每小时 | kg/h |

> 40001~40002 始终返回传感器瞬时流量的固定 L/h 数值，不随 `flow_unit` 改变。OLED 显示按 `flow_unit` 换算：m³/h=`L/h ÷ 1000`，L/min=`L/h ÷ 60`，kg/h=`L/h × medium_density ÷ 1000`。

### 5.3 累积单位 (total_unit, 地址 23)

| 值 | 含义 | 单位 |
|:---:|------|------|
| 0 | 立方米 | m³ |
| 1 | 升 | L |
| 2 | 千克 | kg |
| 3 | 吨 | t |

> 40041~40044 始终返回传感器 BCD 原始累积计数，不随 `total_unit` 改变。OLED 根据源单位标志和 `total_unit` 换算为 m³、L、kg 或 t；kg/t 使用 `medium_density`，结果四舍五入至 0.001 显示单位。

### 5.4 脉冲当量 (pulse_equiv, 地址 76)

| 值 | 含义 | 单位 |
|:---:|------|------|
| 0 | 0.001 | L/Pulse |
| 1 | 0.01 | L/Pulse |
| 2 | 0.1 | L/Pulse |
| 3 | 1 | L/Pulse |
| 4 | 10 | L/Pulse |
| 5 | 100 | L/Pulse |

### 5.5 波特率 (baud_rate, 地址 92)

| 值 | 波特率 |
|:---:|--------|
| 0 | 4800 |
| 1 | 9600 |
| 2 | 19200 |
| 3 | 38400 |
| 4 | 115200 |

### 5.6 语言 (language, 地址 93)

| 值 | 语言 |
|:---:|------|
| 0 | English |
| 1 | 中文 |

---

## 6. 数据类型与字节序

### 6.1 uint16 类型 (单寄存器)

标准 Modbus 大端字节序: 高字节在前，低字节在后。

```
寄存器值 = 0x0001 (流量单位 L/h)
线上字节: [00] [01]
          高位  低位
```

### 6.2 float 类型 (双寄存器, IEEE 754 单精度)

固件使用 `Uart_SendfloatTypeDef` 联合体进行 float 与字节之间的转换:

```c
typedef union {
    uint8_t str[4];  // 小端内存布局: str[0]=LSB, str[3]=MSB
    float   num;
} Uart_SendfloatTypeDef;
```

**线上传输格式** (2 个连续寄存器):

| 寄存器 | 线上字节 | 内容 |
|--------|---------|------|
| N (低位字) | [str[1]] [str[0]] | float 的低 2 字节 (大端字序) |
| N+1 (高位字) | [str[3]] [str[2]] | float 的高 2 字节 (大端字序) |

**示例 — float 1.0**:
```
IEEE 754: 0x3F800000
内存 (小端): str[0]=00 str[1]=00 str[2]=80 str[3]=3F
寄存器 N   = 0x0000  → 线上: [00] [00]
寄存器 N+1 = 0x3F80  → 线上: [3F] [80]
完整线上: 00 00 3F 80
```

**常用 float 编码速查**:

| 数值 | IEEE 754 Hex | 寄存器 N | 寄存器 N+1 | 线上字节 |
|------|:---:|:---:|:---:|------|
| 0.0 | 0x00000000 | 0x0000 | 0x0000 | `00 00 00 00` |
| 1.0 | 0x3F800000 | 0x0000 | 0x3F80 | `00 00 3F 80` |
| 2.0 | 0x40000000 | 0x0000 | 0x4000 | `00 00 00 40` |
| 4.0 | 0x40800000 | 0x0000 | 0x4080 | `00 00 80 40` |
| 20.0 | 0x41A00000 | 0x0000 | 0x41A0 | `00 00 A0 41` |
| 25.0 | 0x41C80000 | 0x0000 | 0x41C8 | `00 00 C8 41` |
| 100.0 | 0x42C80000 | 0x0000 | 0x42C8 | `00 00 C8 42` |
| 101.3 | 0x42CA999A | 0x999A | 0x42CA | `9A 99 CA 42` |
| 1000.0 | 0x447A0000 | 0x0000 | 0x447A | `00 00 7A 44` |

### 6.3 uint64 类型 (累积流量, 4 寄存器)

累积流量使用 4 个寄存器传输，字节顺序为固件内部定义的非标准格式:

```
字节 0: (Cumulativeflow >> 40) & 0xFF
字节 1: (Cumulativeflow >> 32) & 0xFF
字节 2: (Cumulativeflow >> 56) & 0xFF
字节 3: (Cumulativeflow >> 48) & 0xFF
字节 4: (Cumulativeflow >> 8)  & 0xFF
字节 5: (Cumulativeflow >> 0)  & 0xFF
字节 6: (Cumulativeflow >> 24) & 0xFF
字节 7: (Cumulativeflow >> 16) & 0xFF
```

**示例 — 累积流量 = 12345678**:
```
线上字节: 00 00 00 00 61 4E 00 BC
```

---

## 7. 功能码详解与示例

> 以下示例均使用 **默认参数**: 从站地址=0x02, 波特率=115200。
> 帧格式中 `XX XX` 为 CRC16-Modbus 校验 (低字节在前)。

### 7.1 FC03 — 读保持寄存器

**请求帧格式**:

| 字节 | 含义 | 示例值 |
|------|------|--------|
| 0 | 从站地址 | 0x02 |
| 1 | 功能码 | 0x03 |
| 2~3 | 起始地址 (高字节在前) | — |
| 4~5 | 寄存器数量 (高字节在前) | — |
| 6~7 | CRC16 | — |

**响应帧格式**:

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x03) |
| 2 | 后续数据字节数 (= 寄存器数量 × 2) |
| 3~N | 寄存器数据 (每个寄存器 2 字节，高字节在前) |
| N+1~N+2 | CRC16 |

#### 示例 1: 读取瞬时流量 (地址 0, 数量 2)

读取地址 0~1 (40001~40002) 的瞬时流量 float 值。

**请求**:
```
02 03 00 00 00 02 C4 38
```

**响应** (假设瞬时流量 = 123.456 m³/h):
```
02 03 04 E9 79 42 F6 9C 50
│  │  │  ├─────┤ ├─────┤
│  │  │  Reg0   Reg1    → float 123.456
│  │  └─ 数据字节数 = 4
│  └─ 功能码
└─ 从站地址
```

#### 示例 2: 读取累积流量 (地址 40, 数量 4)

```
请求: 02 03 00 28 00 04 C4 32
响应 (累积流量 = 12345678):
02 03 08 00 00 00 00 61 4E 00 BC E4 C9
```

#### 示例 3: 读取通信状态 (地址 60, 数量 1)

```
请求: 02 03 00 3C 00 01 44 35
响应 (通信正常, ModuleState=0):
02 03 02 00 00 FC 44
```

#### 示例 4: 批量读取运行数据 (地址 60, 数量 9)

一次性读取通信状态 + 正向累积 + 反向累积 + 净累积 + DAC 电流。

```
请求: 02 03 00 3C 00 09 45 F3
响应: 02 03 12 [18 字节数据] [CRC_L] [CRC_H]
```

#### 示例 5: 读取运行参数 (地址 22, 数量 8)

读取流量单位 + 累积单位 + 仪表系数 + 介质系数 + 小信号切除。

```
请求: 02 03 00 16 00 08 A5 FB
响应: 02 03 10 [16 字节数据] [CRC_L] [CRC_H]
      └─ 含: uint16 + uint16 + float + float + float
```

#### 示例 6: 读取全部扩展配置 (地址 69, 数量 25)

一次性读取从标准工况到语言的全部配置参数。

```
请求: 02 03 00 45 00 19 95 E6
响应: 02 03 32 [50 字节数据] [CRC_L] [CRC_H]
```

### 7.2 FC06 — 写单个寄存器

**请求/响应帧格式** (响应为请求的回显):

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x06) |
| 2~3 | 寄存器地址 |
| 4~5 | 寄存器值 (高字节在前) |
| 6~7 | CRC16 |

#### 示例 1: 写入流量单位 (uint16, 单次提交)

将流量单位设置为 L/h (枚举值=1)。

```
请求: 02 06 00 16 00 01 A9 FD
响应: 02 06 00 16 00 01 A9 FD   ← 回显
      │  │  ├─────┤ ├─────┤
      │  │  地址=22  值=1(L/h)
      │  └─ FC06
      └─ 从站地址
```

#### 示例 2: 写入 float 参数 — 滤波时间 (分次写入)

将滤波时间设置为 5.0 秒。float `5.0` 编码为: Reg_N=0x0000, Reg_N+1=0x40A0。

**步骤 1 — 写入低位字 (地址 70)**:
```
请求: 02 06 00 46 00 00 68 2C
响应: 02 06 00 46 00 00 68 2C
      (此时数据仅缓存，param_set_filter_time 尚未调用)
```

**步骤 2 — 写入高位字 (地址 71)**:
```
请求: 02 06 00 47 40 A0 08 54
响应: 02 06 00 47 40 A0 08 54
      (此时触发 param_set_filter_time(5.0)，参数生效并持久化)
```

> **重要**: 使用 FC06 写入 float 参数时，**必须先写低位字，再写高位字**。只有高位字到达时才会触发 setter 提交参数。如果只写低位字，参数不会生效。

#### 示例 3: 写入模拟总开关

开启模拟模式:

```
请求: 02 06 00 30 00 01 48 36
响应: 02 06 00 30 00 01 48 36
```

关闭模拟模式:

```
请求: 02 06 00 30 00 00 89 F6
```

> 模拟总开关 (0xFF00 或 0x0001 均为 ON，0x0000 为 OFF)。

#### 示例 4: 修改通信地址

将设备地址从 2 改为 3。

```
请求: 02 06 00 5B 00 03 B8 2B
响应: 02 06 00 5B 00 03 B8 2B
      (地址已立即生效！后续通信需使用地址 3)
```

> **警告**: 写入新地址后，MCU 立即使用新地址匹配 Modbus 帧。当前连接断开。上位机必须切换到新地址才能继续通信。

#### 示例 5: 写入标准工况

将标准工况设置为 101.3KPa/20℃ (枚举值=1)。

```
请求: 02 06 00 45 00 01 59 EC
响应: 02 06 00 45 00 01 59 EC
```

### 7.3 FC10 (0x10) — 写多个寄存器

**请求帧格式**:

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x10) |
| 2~3 | 起始地址 |
| 4~5 | 寄存器数量 |
| 6 | 后续数据字节数 (= 寄存器数量 × 2) |
| 7~N | 寄存器数据 |
| N+1~N+2 | CRC16 |

**响应帧格式**:

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x10) |
| 2~3 | 起始地址 |
| 4~5 | 寄存器数量 |
| 6~7 | CRC16 |

#### 示例 1: 写入 float 参数 (推荐方式)

将滤波时间设置为 5.0 秒，使用 FC10 一次写入 2 个寄存器。

```
请求: 02 10 00 46 00 02 04 00 00 40 A0 49 49
      │  │  ├─────┤ ├─────┤ │  ├─────┤ ├─────┤
      │  │  addr=70  qty=2  4B Reg0=0000 Reg1=40A0
      │  └─ FC10
      └─ 从站地址

响应: 02 10 00 46 00 02 51 F8
      (param_set_filter_time(5.0) 已触发)
```

> **推荐**: 写入 float 参数时，优先使用 FC10 一次写入 2 个寄存器，避免 FC06 分次写入的时序问题。

#### 示例 2: 批量写入运行参数

同时写入流量单位=1(L/h) 和累积单位=0(m³)。

```
请求: 02 10 00 16 00 02 04 00 01 00 00 2C 0D
      │  │  ├─────┤ ├─────┤ │  ├─────┤ ├─────┤
      │  │  addr=22  qty=2  4B flow_unit=1 total_unit=0
      │  └─ FC10
      └─ 从站地址

响应: 02 10 00 16 00 02 01 38
```

### 7.4 FC01 — 读线圈

**请求帧格式**:

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x01) |
| 2~3 | 线圈起始地址 |
| 4~5 | 线圈数量 |
| 6~7 | CRC16 |

**响应帧格式**:

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x01) |
| 2 | 后续数据字节数 |
| 3~N | 线圈状态 (每 bit 对应一个线圈, LSB 在前) |
| N+1~N+2 | CRC16 |

#### 示例: 读取线圈 0~5 状态

```
请求: 02 01 00 00 00 06 BC 3B
响应: 02 01 01 XX XX XX
      │  │  │  └─ 线圈状态字节 (bit0=线圈0, bit1=线圈1, ...)
      │  │  └─ 数据字节数 = 1
      │  └─ FC01
      └─ 从站地址
```

若响应数据为 `0x09` (二进制 00001001)，表示线圈 0 (DAC 使能) 和线圈 3 (累积清零) 处于 ON 状态。

### 7.5 FC05 — 写单个线圈

**请求/响应帧格式** (响应为请求的回显):

| 字节 | 含义 |
|------|------|
| 0 | 从站地址 |
| 1 | 功能码 (0x05) |
| 2~3 | 线圈地址 |
| 4~5 | 线圈值 (0xFF00=ON, 0x0000=OFF) |
| 6~7 | CRC16 |

#### 示例 1: 使能 DAC 校准模式 (线圈 0 ON)

```
请求: 02 05 00 00 FF 00 8C 09
响应: 02 05 00 00 FF 00 8C 09
      (CalEnabledFlag=1, DAC 校准模式已开启)
```

#### 示例 2: 强制 DAC 零点输出 (线圈 1 ON)

```
请求: 02 05 00 01 FF 00 DD C9
响应: 02 05 00 01 FF 00 DD C9
      (DacValue=DacZeroValue, DAC 输出 4mA)
```

取消强制 (线圈 1 OFF):

```
请求: 02 05 00 01 00 00 9C 09
响应: 02 05 00 01 00 00 9C 09
      (恢复正常 DAC 输出)
```

#### 示例 3: 累积流量清零 (线圈 3 ON)

```
请求: 02 05 00 03 FF 00 7C 09
响应: 02 05 00 03 FF 00 7C 09
      (向 UFL-1A 模组发送累积清零命令)
```

#### 示例 4: 流量模组复位 (线圈 4 ON)

```
请求: 02 05 00 04 FF 00 CD C8
响应: 02 05 00 04 FF 00 CD C8
      (向 UFL-1A 模组发送复位命令)
```

---

## 8. 典型应用场景

### 场景 1: 上位机实时监控

周期性 (如 1 秒) 读取关键运行数据:

```
FC03 读地址 0, 数量 6    → 瞬时流量 + 温度 + 压力
FC03 读地址 40, 数量 4   → 累积流量 (uint64)
FC03 读地址 60, 数量 9   → 通信状态 + 正/反/净累积 + DAC 电流
```

### 场景 2: 远程参数配置

修改介质密度为 998.0 kg/m³ (地址 77~78):

```
FC10 写地址 77, 数量 2, 数据 [XX XX XX XX]   ← float 998.0 的编码
```

### 场景 3: DAC 校准流程

```
步骤 1: FC05 线圈 0 = ON     → 使能 DAC 校准模式
步骤 2: FC05 线圈 1 = ON     → 强制输出 4mA
步骤 3: 用万用表测量实际电流
步骤 4: FC10 写地址 20       → 写入实测 4mA 对应的 DacZeroValue
步骤 5: FC05 线圈 1 = OFF    → 取消零点强制
步骤 6: FC05 线圈 2 = ON     → 强制输出 20mA
步骤 7: 用万用表测量实际电流
步骤 8: FC10 写地址 21       → 写入实测 20mA 对应的 DacFullValue
步骤 9: FC05 线圈 2 = OFF    → 取消满度强制
步骤 10: FC05 线圈 0 = OFF   → 退出 DAC 校准模式
```

### 场景 4: 模拟调试

```
步骤 1: FC06 写地址 50, 数量 2 → 写入模拟瞬时流量 (如 50.0)
步骤 2: FC06 写地址 48 = 1     → 开启模拟总开关
        (此时 OLED 显示 50.0, DAC 按模拟值输出)
步骤 3: FC06 写地址 48 = 0     → 关闭模拟，恢复真实数据
```

### 场景 5: 更改设备通信参数

```
# 将设备地址从 2 改为 5
FC06 写地址 91, 值 5
→ 响应帧使用旧地址 2 返回
→ 之后上位机必须使用地址 5 通信

# 将波特率从 115200 改为 9600
FC06 写地址 92, 值 1
→ 响应帧以 115200 波特率发送完成后再切换
→ 上位机收到响应后切换自身波特率为 9600
→ 之后以 9600 波特率通信
```

---

## 9. 通信异常处理

### 9.1 无响应

可能原因:
- 从站地址不匹配 (检查地址是否正确)
- 波特率/校验位/停止位不匹配
- RS-485 接线错误 (A/B 是否反接)
- 设备未上电或通信线未连接

### 9.2 CRC 校验错误

固件对 CRC 校验错误的帧直接丢弃，不返回任何响应。上位机应实现超时重发机制。

### 9.3 地址越界

- 读取未定义的地址范围，返回数据为 0x0000
- 写入未定义的地址范围，MCU 回显响应帧但不修改任何数据

### 9.4 寄存器数量限制

- FC03 单次最多读取 62 个寄存器 (缓冲区溢出防护)
- FC04 单次最多读取 62 个寄存器
- 超过限制时自动截断为 62

### 9.5 通信超时

固件中 TIM3 每 500ms 检查一次 UFL-1A 模组通信状态，连续 5 次超时 (2.5 秒) 将 `ModuleState` 置 1。上位机可通过读取地址 60 (40061) 获取通信状态。

---

## 10. 注意事项

1. **float 写入顺序**: 使用 FC06 写 float 时，必须先写低位字 (偶数地址) 再写高位字 (奇数地址)。建议使用 FC10 一次写入 2 个寄存器。

2. **通信地址安全**: 修改通信地址后立即生效且持久化。建议上位机在修改前记录新地址，避免设备"失联"。

3. **波特率安全**: 波特率修改采用延迟应用机制，确保当前帧的响应在旧波特率下发送完成。上位机收到响应后应切换自身波特率。

4. **模拟参数掉电恢复**: 模拟参数 (地址 48~55) 仅存于 RAM，设备断电重启后自动恢复为真实传感器数据模式。

5. **累积流量格式**: 地址 40 的累积流量使用 uint64 (4 寄存器) 格式，字节顺序为固件特殊定义，非标准 Modbus 64 位整数编码。

6. **地址 40 的特殊性**: 地址 40 独立于 InputBuffer 区域，仅在 `startaddress == 40` 时触发特殊路径，不可与地址 0~18 批量读取混用。

7. **Flash 写入寿命**: 参数修改会触发 Flash 持久化。STM32 Flash 典型擦写寿命为 10000 次，建议上位机不要高频循环写入同一参数。

8. **看门狗**: 固件启用 IWDG 看门狗 (超时约 1 秒)。如果上位机操作导致 Modbus 处理时间过长 (理论上不会发生)，设备将自动复位。

---

## 附录 A: 完整寄存器速查表

| 寄存器地址 | Modbus 4X | 参数名 | 类型 | 读写 | 默认值 |
|:---:|:---:|--------|:---:|:---:|:---:|
| 0~1 | 40001~40002 | 瞬时流量 | float | R | 固定 L/h，不随 40023 改变 |
| 2~3 | 40003~40004 | 温度 (℃) | float | R | — |
| 4~5 | 40005~40006 | 压力 (KPa) | float | R | — |
| 20 | 40021 | DAC 零点值 | uint16 | R/W | — |
| 21 | 40022 | DAC 满度值 | uint16 | R/W | — |
| 22 | 40023 | 流量单位 | uint16 | R/W | 0 |
| 23 | 40024 | 累积单位 | uint16 | R/W | 0 |
| 24~25 | 40025~40026 | 仪表系数 | float | R/W | 1.000 |
| 26~27 | 40027~40028 | 介质系数 | float | R/W | 1.000 |
| 28~29 | 40029~40030 | 小信号切除 (%) | float | R/W | 2.0 |
| 30~31 | 40031~40032 | 4mA 量程值 | float | R/W | 0.0 |
| 32~33 | 40033~40034 | 20mA 量程值 | float | R/W | 100.0 |
| 40~43 | 40041~40044 | 累积流量 | uint64 | R | BCD 原始计数，不随 40024 改变 |
| 48 | 40049 | 模拟总开关 | uint16 | R/W | 0 |
| 50~51 | 40051~40052 | 模拟瞬时流量 | float | R/W | 0.0 |
| 52~53 | 40053~40054 | 模拟温度 | float | R/W | 0.0 |
| 54~55 | 40055~40056 | 模拟累积流量 | float | R/W | 0.0 |
| 60 | 40061 | 通信状态 | uint16 | R | — |
| 61~62 | 40062~40063 | 正向累积 | float | R | 0.0 |
| 63~64 | 40064~40065 | 反向累积 | float | R | 0.0 |
| 65~66 | 40066~40067 | 净累积 | float | R | 0.0 |
| 67~68 | 40068~40069 | DAC 电流 (mA) | float | R | 4.0 |
| 69 | 40070 | 标准工况 | uint16 | R/W | 0 |
| 70~71 | 40071~40072 | 滤波参数 (秒) | float | R/W | 1.0 |
| 72~73 | 40073~40074 | 阻尼时间 (秒) | float | R/W | 1.0 |
| 74~75 | 40075~40076 | 频率输出 (Hz) | float | R/W | 1000.0 |
| 76 | 40077 | 脉冲当量 | uint16 | R/W | 0 |
| 77~78 | 40078~40079 | 介质密度 (kg/m³) | float | R/W | 1000.0 |
| 79~80 | 40080~40081 | 管径 (mm) | float | R/W | 25.0 |
| 81~82 | 40082~40083 | 气参压力 (KPa) | float | R/W | 101.3 |
| 83~84 | 40084~40085 | 气参温度 (℃) | float | R/W | 20.0 |
| 85~86 | 40086~40087 | 雷诺系数 | float | R/W | 1.000 |
| 87~88 | 40088~40089 | 累积系数 | float | R/W | 1.000 |
| 89~90 | 40090~40091 | 预设总量 | float | R/W | 0.0 |
| 91 | 40092 | 通信地址 | uint16 | R/W | 2 |
| 92 | 40093 | 波特率索引 | uint16 | R/W | 4 |
| 93 | 40094 | 语言 | uint16 | R/W | 0 |

## 附录 B: 线圈速查表

| 线圈地址 | 功能 | ON 值 | OFF 值 | 自复位 |
|:---:|------|:---:|:---:|:---:|
| 0 | DAC 输出使能 | 0xFF00 | 0x0000 | 否 |
| 1 | 强制 DAC 零点 (4mA) | 0xFF00 | 0x0000 | 否 |
| 2 | 强制 DAC 满度 (20mA) | 0xFF00 | 0x0000 | 否 |
| 3 | 累积流量清零 | 0xFF00 | 0x0000 | 是 (命令型) |
| 4 | 流量模组复位 | 0xFF00 | 0x0000 | 是 (命令型) |
| 5 | 被动读取模式 | 0xFF00 | 0x0000 | 否 |

---

**文档版本**: v2.3.0
**作者**: LDL
**生成工具**: Claude Code
